<h2><%= @room.name %></h2>
<%= subscribe_to "/rooms/#{@room.id}" %>
<%= form_tag({:controller => 'message', :action => 'create.js'}, :remote => true) do %>
  <div class="field">
    <%= text_field_tag 'message' %>
  </div>
  <div class="field">
    <%= hidden_field_tag 'room_id', @room.id %>
  </div>
  <%= submit_tag 'POST' %>
<% end %>
<table id="chat">
</table>

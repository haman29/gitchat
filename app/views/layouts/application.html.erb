<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "GitChat" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>


    <%= stylesheet_link_tag "application", :media => "all" %>
  </head>
  <body>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="brand" href="/">GitChat</a>
          <div class="container-fluid">
            <ul class="nav">
              <% if session[:access_token] then %>
                <li><%= link_to "NEW", new_room_path  %></li>
                <li><%= link_to "LOGOUT", '/logout' %></li>
              <% else %>
                <li><%= link_to "LOGIN", @github_login_url %></li>
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
        <% if @user then %>
        <div class="span2">
          <div class="well sidebar-nav">
            <ul class="nav nav-list">
              <li class="nav-header">YOUR PROFILE</li>
              <li><%= image_tag @user.avatar_url %></li>
              <li><%= @user.name %></li>
              <li><%= link_to "https://github.com/#{@user.login}" %></li>
            </ul>
          </div><!--/.well -->
        </div>
        <% end %>
        <div class="span7">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
        <%= yield :members %>
      </div><!--/row-->

      <footer>
        <p>&copy; 2013 GitChat. All rights reserved.</p>
        <p>author: <%= link_to '@haman29', 'https://twitter.com/haman29' %></p>
      </footer>
    </div> <!-- /container -->
  </body>
</html>
